# Default: provision THIS machine (the VPS you're SSH'd into).
localhost ansible_connection=local

# ---------------------------------------------------------------------------
# To instead drive a remote box from your laptop (the "classic" Ansible model),
# comment out the line above and add the server here, then run the playbook
# from your laptop with this same repo:
#
# [vps]
# my-contabo ansible_host=194.55.xx.xx ansible_user=root
# ---------------------------------------------------------------------------
